import { environmentVariables } from 'mentor-ship-frontend/helpers/environment-variables';
import { module, test } from 'qunit';

module('Unit | Helper | environment variables');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = environmentVariables([42]);
  assert.ok(result);
});
