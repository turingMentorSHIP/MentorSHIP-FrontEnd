import { getCookie } from 'mentor-ship-frontend/helpers/get-cookie';
import { module, test } from 'qunit';

module('Unit | Helper | get cookie');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getCookie([42]);
  assert.ok(result);
});
